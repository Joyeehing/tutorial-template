Installation
============
To use Thryft, first install it from the Github repository:

.. code-block:: console

   git clone https://github.com/setap-4c/Group-Project.git

Then, install the required packages using the terminal:

.. code-block:: console

   pub get

Finally, run the application using the terminal:

.. code-block:: console

   flutter run

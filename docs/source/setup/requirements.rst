Requirements
============

- The application requires Flutter SDK version 3.35 or above, which includes Dart 3.9 or above as part of the development environment.

- The system depends on several Flutter packages such as Provider for state management, GoRouter for navigation, image handling libraries, and local storage utilities to support core functionality.

- Backend services are supported using Supabase, which provides authentication and database capabilities required for the application to function.
